function SUMA() {
    let "SUMA=($numero + $aSumar)"
}
function RESTA() {
    let "RESTA=($numero - $aRestar)"
}
function MULTIPLICAR() {
    let "MULTIPLICAR=($numero * $factor)"
}
function DIVIDIR() {
    let "DIVIDIR=($numero / $divisor)"
}